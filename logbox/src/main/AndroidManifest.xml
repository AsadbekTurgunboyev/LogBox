<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>

        <activity
            android:name=".ui.ShowLogDialogActivity"
            android:theme="@style/Theme.AppCompat.Dialog"
            android:exported="true" />

        <provider
            android:name="com.asd.logbox.provider.LogBoxInitProvider"
            android:authorities="${applicationId}.logboxinitprovider"
            android:exported="false" />
    </application>
</manifest>